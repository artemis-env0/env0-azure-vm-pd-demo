settings:
  environmentRemovalStrategy: detach

environments:
  network:
    name: "Env0 Azure Network AGA"
    templateName: "Env0 Azure Network AGA"
    workspace: "env0-live-demo-aga-network"
    revision: "v1.0.1"
    requiresApproval: false

  compute:
    name: "Env0 Azure Compute AGA"
    templateName: "Env0 Azure Compute AGA"
    workspace: "env0-live-demo-aga-compute"
    revision: "v1.0.1"
    requiresApproval: false
    needs:
      - network
